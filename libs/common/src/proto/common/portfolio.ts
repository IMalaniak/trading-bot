// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               v7.34.1
// source: common/portfolio.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Instrument } from "./instrument";

export const protobufPackage = "tradingbot.common";

/** Position represents an open position in the portfolio. */
export interface Position {
  portfolioId: string;
  instrument?:
    | Instrument
    | undefined;
  /** positive for net long, negative for net short */
  quantity: string;
  averageEntryPrice: string;
  exposureNotional: string;
  lastFillId: string;
  updatedAt: string;
}

/** Portfolio summary contains MVP exposure/read-model state. */
export interface PortfolioSummary {
  portfolioId: string;
  name: string;
  isActive: boolean;
  exposureCapNotional: string;
  aggregateExposureNotional: string;
  openPositionCount: number;
  updatedAt: string;
}

function createBasePosition(): Position {
  return {
    portfolioId: "",
    instrument: undefined,
    quantity: "",
    averageEntryPrice: "",
    exposureNotional: "",
    lastFillId: "",
    updatedAt: "",
  };
}

export const Position: MessageFns<Position> = {
  encode(message: Position, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.portfolioId !== "") {
      writer.uint32(10).string(message.portfolioId);
    }
    if (message.instrument !== undefined) {
      Instrument.encode(message.instrument, writer.uint32(18).fork()).join();
    }
    if (message.quantity !== "") {
      writer.uint32(26).string(message.quantity);
    }
    if (message.averageEntryPrice !== "") {
      writer.uint32(34).string(message.averageEntryPrice);
    }
    if (message.exposureNotional !== "") {
      writer.uint32(42).string(message.exposureNotional);
    }
    if (message.lastFillId !== "") {
      writer.uint32(50).string(message.lastFillId);
    }
    if (message.updatedAt !== "") {
      writer.uint32(58).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Position {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.portfolioId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instrument = Instrument.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.averageEntryPrice = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.exposureNotional = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.lastFillId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Position>, I>>(base?: I): Position {
    return Position.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Position>, I>>(object: I): Position {
    const message = createBasePosition();
    message.portfolioId = object.portfolioId ?? "";
    message.instrument = (object.instrument !== undefined && object.instrument !== null)
      ? Instrument.fromPartial(object.instrument)
      : undefined;
    message.quantity = object.quantity ?? "";
    message.averageEntryPrice = object.averageEntryPrice ?? "";
    message.exposureNotional = object.exposureNotional ?? "";
    message.lastFillId = object.lastFillId ?? "";
    message.updatedAt = object.updatedAt ?? "";
    return message;
  },
};

function createBasePortfolioSummary(): PortfolioSummary {
  return {
    portfolioId: "",
    name: "",
    isActive: false,
    exposureCapNotional: "",
    aggregateExposureNotional: "",
    openPositionCount: 0,
    updatedAt: "",
  };
}

export const PortfolioSummary: MessageFns<PortfolioSummary> = {
  encode(message: PortfolioSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.portfolioId !== "") {
      writer.uint32(10).string(message.portfolioId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.isActive !== false) {
      writer.uint32(24).bool(message.isActive);
    }
    if (message.exposureCapNotional !== "") {
      writer.uint32(34).string(message.exposureCapNotional);
    }
    if (message.aggregateExposureNotional !== "") {
      writer.uint32(42).string(message.aggregateExposureNotional);
    }
    if (message.openPositionCount !== 0) {
      writer.uint32(48).int32(message.openPositionCount);
    }
    if (message.updatedAt !== "") {
      writer.uint32(58).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortfolioSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortfolioSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.portfolioId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.exposureCapNotional = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.aggregateExposureNotional = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.openPositionCount = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PortfolioSummary>, I>>(base?: I): PortfolioSummary {
    return PortfolioSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PortfolioSummary>, I>>(object: I): PortfolioSummary {
    const message = createBasePortfolioSummary();
    message.portfolioId = object.portfolioId ?? "";
    message.name = object.name ?? "";
    message.isActive = object.isActive ?? false;
    message.exposureCapNotional = object.exposureCapNotional ?? "";
    message.aggregateExposureNotional = object.aggregateExposureNotional ?? "";
    message.openPositionCount = object.openPositionCount ?? 0;
    message.updatedAt = object.updatedAt ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
