// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.32.1
// source: services/prediction_engine.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Signal } from "../common/signal";

export const protobufPackage = "tradingbot.prediction";

/** Request to fetch latest signals. */
export interface GetLatestSignalsRequest {
  /** Optional filter: instrument_id to return signals for. Empty means all instruments. */
  instrumentId: string;
  /** Optional: limit number of returned signals. 0 means server default. */
  limit: number;
}

/** Response containing latest signals. */
export interface GetLatestSignalsResponse {
  /**
   * Reference the message defined in proto/common/signal.proto which uses
   * package `tradingbot.common`.
   */
  signals: Signal[];
}

function createBaseGetLatestSignalsRequest(): GetLatestSignalsRequest {
  return { instrumentId: "", limit: 0 };
}

export const GetLatestSignalsRequest: MessageFns<GetLatestSignalsRequest> = {
  encode(message: GetLatestSignalsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instrumentId !== "") {
      writer.uint32(10).string(message.instrumentId);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLatestSignalsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLatestSignalsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instrumentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetLatestSignalsRequest>, I>>(base?: I): GetLatestSignalsRequest {
    return GetLatestSignalsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLatestSignalsRequest>, I>>(object: I): GetLatestSignalsRequest {
    const message = createBaseGetLatestSignalsRequest();
    message.instrumentId = object.instrumentId ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetLatestSignalsResponse(): GetLatestSignalsResponse {
  return { signals: [] };
}

export const GetLatestSignalsResponse: MessageFns<GetLatestSignalsResponse> = {
  encode(message: GetLatestSignalsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.signals) {
      Signal.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLatestSignalsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLatestSignalsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signals.push(Signal.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetLatestSignalsResponse>, I>>(base?: I): GetLatestSignalsResponse {
    return GetLatestSignalsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLatestSignalsResponse>, I>>(object: I): GetLatestSignalsResponse {
    const message = createBaseGetLatestSignalsResponse();
    message.signals = object.signals?.map((e) => Signal.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
