# Local API Gateway runtime config.
#
# Shared gRPC URLs live in the workspace-root .env. Only API Gateway-owned
# HTTP/CORS values belong here.

PORT=3000
API_GATEWAY_CORS_ORIGINS=http://localhost:4200,http://127.0.0.1:4200
